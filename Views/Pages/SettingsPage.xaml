<Page
    x:Class="OneDesk.Views.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:OneDesk.Helpers"
    xmlns:local="clr-namespace:OneDesk.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="SettingsPage"
    d:DataContext="{d:DesignInstance local:SettingsPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d" Cursor="">
    <Page.Resources>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
    </Page.Resources>

    <ui:DynamicScrollViewer>
        <StackPanel Margin="0,0,0,24">
            <TextBlock FontSize="20" FontWeight="Medium" Text="个性化" />

            <!-- Follow system theme toggle -->
            <StackPanel Orientation="Horizontal" Margin="0,12,0,0" VerticalAlignment="Center">
                <TextBlock Text="跟随系统主题" VerticalAlignment="Center" />
                <ui:ToggleSwitch Margin="12,0,0,0"
                                  IsChecked="{Binding ViewModel.Config.FollowSystemTheme, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ViewModel.Config.FollowSystemTheme}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock Margin="0,12,0,0" Text="主题" />
                <!-- Theme options hidden when FollowSystemTheme = true -->
                <RadioButton
                    Margin="0,12,0,0"
                    Command="{Binding ViewModel.ChangeThemeCommand, Mode=OneWay}"
                    CommandParameter="theme_light"
                    Content="浅色"
                    GroupName="themeSelect"
                    IsChecked="{Binding ViewModel.CurrentTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Light, Mode=OneWay}" />
                <RadioButton
                    Margin="0,8,0,0"
                    Command="{Binding ViewModel.ChangeThemeCommand, Mode=OneWay}"
                    CommandParameter="theme_dark"
                    Content="深色"
                    GroupName="themeSelect"
                    IsChecked="{Binding ViewModel.CurrentTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Dark, Mode=OneWay}" />
            </StackPanel>

            <TextBlock Margin="0,24,0,0" FontSize="20" FontWeight="Medium" Text="应用配置" />

            <!-- ClientId -->
            <TextBlock Margin="0,12,0,0" Text="Client ID" />
            <ui:TextBox Margin="0,4,0,0" Text="{Binding ViewModel.Config.ClientId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="输入 Client ID" HorizontalAlignment="Left" Width="339" />

            <!-- TenantId -->
            <TextBlock Margin="0,12,0,0" Text="Tenant ID" />
            <ui:TextBox Margin="0,4,0,0" Text="{Binding ViewModel.Config.TenantId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="输入 Tenant ID" HorizontalAlignment="Left" Width="339" />

            <!-- CredentialFolderPath -->
            <TextBlock Margin="0,12,0,0" Text="凭据文件夹路径" />
            <ui:TextBox Margin="0,4,0,0" Text="{Binding ViewModel.Config.CredentialFolderPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="文件夹路径" HorizontalAlignment="Left" Width="339" />

            <TextBlock Margin="0,24,0,0" FontSize="20" FontWeight="Medium" Text="关于 OneDesk" />
            <TextBlock Margin="0,12,0,0" Text="{Binding ViewModel.AppVersion, Mode=OneWay}" />
        </StackPanel>
    </ui:DynamicScrollViewer>
</Page>
